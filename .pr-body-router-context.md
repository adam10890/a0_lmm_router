## Summary

תיקון לשגיאת `exceed_context_size_error` במודלים מקומיים דרך llama.cpp Router (`n_ctx=65536`).

Agent Zero דוחס **רק היסטוריית שיחה** מול `ctx_length × ctx_history` בשלב `message_loop_prompts_before` — **לפני** בניית ה-system prompt. הראוטר בודק את **כל** הבקשה (system + history + extras + מקום לתשובה). לכן שיחה ארוכה יכולה לעבור את בדיקת הדחיסה הפנימית של A0 ועדיין לחרוג ב־~2k טוקנים (למשל 67,569 > 65,536).

## מה נוסף

- **`helpers/router_context.py`** — קריאת `n_ctx` חי מ־`GET /v1/models`, עם fallback ל־`ROUTER_CTX_SIZE` / הגדרות slot / preset.
- **`extensions/python/message_loop_prompts_after/_20_router_context_guard.py`** — רץ **אחרי** `get_system_prompt()`:
  1. מחשב תקציב היסטוריה: `int(n_ctx × 0.9) − system_tokens − extras_estimate − 8192`
  2. מחליף זמנית את `History._get_ctx_size_for_history()` לתקציב הזה
  3. מפעיל `history.compress()` עד שההיסטוריה נכנסת לחלון
  4. מרענן `loop_data.history_output`
- פעיל רק כש־`provider: lmm_router` (לא משפיע על OpenAI/Codex וכו').
- לוג: **"LMM Router context guard"** ב־UI כשמתבצעת דחיסה.

## למה לא מספיק רק `ctx_history` ב־preset?

`ctx_history: 0.55` על 65536 ≈ 36k טוקנים להיסטוריה — אבל system prompt + memories יכולים להוסיף עשרות אלפי טוקנים **אחרי** הדחיסה. ה-extension סוגר את הפער.

## Test plan

- [x] `pytest tests/test_router_context_guard.py` (4 tests)
- [ ] שיחה ארוכה עם preset **Local Fleet (llama.cpp)** — לוודא שאין `exceed_context_size_error`
- [ ] בלוג הסוכן: הופעת "LMM Router context guard" כשההיסטוריה חורגת
- [ ] `docker restart agent-zero-2` אחרי merge כדי לטעון את ה-extension
